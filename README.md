# auto_deploy_cpanel
Complete guide to auto-deploy your cPanel project using GitHub Webhooks and SSH. Learn how to connect GitHub with cPanel, configure SSH keys, set up the webhook, and automatically deploy the latest code on every push—no manual upload required.
